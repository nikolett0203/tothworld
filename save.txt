.bun-top {
    position: fixed;
    z-index: 10;

    /* place top left corner in center of screen */
    top: 50%;
    left: 50%;

    /* offset by half height and width for perfect centering */
    transform: translate(-50%, -50%);

    text-align: center;
}

.title {
    /* title was wonky without this */
    position: relative;
    left: 50%;
    transform: translateX(-50%);

    font-family: 'Urbanist', sans-serif;
    font-weight: 500;
    color: white;

    /* responsive size: at least 3rem, grow with screen (8vw) but never exceed 6rem */
    font-size: clamp(40px, 8vw, 60px);

    margin-bottom: 2rem;
    letter-spacing: 4px;  

    animation: float 6s ease-in-out infinite;
}

.subtitle {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 20px;

    font-family: 'Urbanist', sans-serif;
    font-weight: 300;
    color: rgba(255, 255, 255, 0.85);
    letter-spacing: 1px;
    font-size: clamp(15px, 2vw, 18px);
}

@keyframes float {
    0% {
        transform: translate(-50%, -50%) translateY(0px);
    }

    /* shift it down 10px to make it look like it's bobbing */
    50% {
        transform: translate(-50%, -50%) translateY(-10px);
    }

    100% {
        transform: translate(-50%, -50%) translateY(0px);
    }
}